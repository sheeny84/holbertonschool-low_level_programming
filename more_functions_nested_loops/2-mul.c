#include "main.h"

/**
 * mul - multiply two integers
 * @a: the first integer for the multiplication
 * @b: the second integer for the multiplication
 *
 * Return: the result of the multiplication
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
